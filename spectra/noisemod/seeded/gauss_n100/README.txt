these were runs done by multiplying the amplitude of the lensing template noise maps by 100x, and adding them to the comb spectra as well.

results were expected (see mlsearch). however we want to avoid keeping the modified noise in the noise spectra (as it enters the bpcm/covariance matrix).
